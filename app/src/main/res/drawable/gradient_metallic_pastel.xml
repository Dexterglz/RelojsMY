<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Degradado metálico pastel -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="45"
                android:startColor="#FFB8E6FF"
                android:centerColor="#FFFFE0B8"
                android:endColor="#FFB8FFE8"
            android:centerX="0.3"
            android:centerY="0.3" />
        </shape>
    </item>

    <!-- Tu icono SVG -->
    <item
        android:drawable="@drawable/solid_heartbeat"
        android:tint="@color/white" />
</layer-list>